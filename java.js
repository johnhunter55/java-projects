function alertMessage(message) {
  alert(message);
}

alertMessage("Hello, World!");

const name = "John";

function getWelcomeMessage(name) {
  return "welcome to my coding world, " + name + "!";
}

console.log(getWelcomeMessage(name));

const a = 20;
const b = 10;

function divide(a, b) {
  return a / b;
}

function multiply(a, b) {
  return a * b;
}
